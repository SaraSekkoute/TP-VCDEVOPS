pipeline {
    agent any
    environment {
        DOCKER_CREDENTIALS = credentials('docker-credentials-id')
    }
    stages {
        stage('Build and Push') {
            steps {
                script {
                    docker.build('myimage').push()
                }
            }
        }
    }
}
